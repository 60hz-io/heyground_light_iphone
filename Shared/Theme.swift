import SwiftUI

/// 안드로이드 버전·웹(qr.60hz.cc)과 같은 브랜드 색.
enum Theme {
    static let navy = Color(red: 0x01 / 255, green: 0x3D / 255, blue: 0x7E / 255)
    static let expired = Color(red: 0xFF / 255, green: 0x6B / 255, blue: 0x6B / 255)
    static let logout = Color(red: 0xFF / 255, green: 0x52 / 255, blue: 0x52 / 255)
}
